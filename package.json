{
  "name": "y",
  "version": "y",
  "description": "y",
  "main": "y",
  "repository": "y",
  "author": "y",
  "license": "y",
  "scripts": {
    "eth": "docker compose up",
    "guard": "docker rm -vf $(docker ps -a | grep guardian | awk '{print $1}') && docker-compose -f docker-compose-guardian.yaml -- up"
  }
}
